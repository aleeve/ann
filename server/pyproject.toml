[tool.poetry]
name = "server"
version = "0.1.0"
description = ""
authors = ["Alex Evert <alex.evert@eghed.se>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flwr = "^1.8.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
