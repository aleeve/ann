[package]
name = "proto"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.12.6"
tonic = "0.11.0"

[build-dependencies]
tonic-build = { version = "0.11.0", default-features = false, features = [
    "prost",
] }

